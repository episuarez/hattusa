configuration = {
    # Debug mode, to check any error in the application.
    "DEBUG": False,

    # Required paths for application. You must have write permissions.
    "PATH_BOOKS": "/books/",
    "PATH_COVERS": "static/covers/",
    "PATH_TEMP": "/static/temp/",

    # Running in desktop application mode. Not compatible with debug mode.
    "MODE_DESKTOP": False
}